import pandas as pd

"""
Combines column1 and column2 in the data frame, placing the results into outputColumn

Returns nothing
"""
def merge_columns(data: pd.DataFrame, column1: str, column2: str, outputColumn: str):
    raise "TODO"